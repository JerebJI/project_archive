#include<stdio.h>
enum letni_cas {jesen,zima,poletje,pomlad};
enum vrsta {k1,k2,k3,k4};
enum barva {c,r,b,z};
strukt karta() {
	vrsta v;
	barva b;
};
int main() {
	enum karta[10]=
	enum letni_cas x=zima;
	for(enum letni_cas i=jesen; i<=pomlad; i++)printf("%d\n",i);
	return 0;
}
